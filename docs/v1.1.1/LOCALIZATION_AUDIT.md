# Localization audit

The baseline uses hard-coded English copy and an `AppCopy` helper, not yet a String Catalog. The first P0 views use the approved English source terms while catalog migration and zh-Hans review remain required before v1.1.1 completion. This document must be updated with string-catalog coverage, Dynamic Type checks, truncation checks, and locale-specific date/number validation in the localization phase.
